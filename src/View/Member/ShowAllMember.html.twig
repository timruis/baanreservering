{% extends 'UserLogedin.html.twig' %}

        {% block title %}IZEW - {{ Title }}{% endblock %}


        {% block stylesheets %}
            {{ encore_entry_link_tags('DataTables') }}


            {{ parent() }}
        {% endblock %}

        {% block body %}

            <div class="content mt-3">
                <div class="animated fadeIn">
                    <div class="card">
                        <div class="card-header">
                            <strong class="card-title">Leden register</strong>
                        </div>
                        <div class="card-body">
                            <!-- Credit Card -->

                            <a href="{{ path('new-member-Registry') }}" class="btn btn-outline-primary pull-right">
                                Create <span class="fa fa-plus-circle"></span>
                            </a>
                            <div style="padding:1em;overflow-x: scroll;">
                                <div class="card-title">
                                    <h3 class="text-center">Tabel van Leden</h3>
                                </div>
                                <hr>
                                <table id="bootstrap-data-table-export" class="table table-striped table-bordered">
                                    <thead>
                                    <tr>
                                        <th>Members ID</th>
                                        <th>Foto</th>
                                        <th>voornaam</th>
                                        <th>Achternaam</th>
                                        <th>Email</th>
                                        <th>Telefoon</th>
                                        {% if is_granted('ROLE_USERMANAGEMENT') or is_granted('ROLE_ADMIN') %}
                                            <th>Betaald</th>
                                            <th>Actief</th>
                                        {% endif %}
                                            <th>Change</th>
                                        {% if is_granted('ROLE_USERMANAGEMENT') or is_granted('ROLE_ADMIN') %}
                                            <th>Delete</th>
                                        {% endif %}
                                    </tr>
                                    </thead>
                                    <tbody>
                                    {% for Member in Members %}
                                        <tr class="tbrw{{ Member.Id }}
                                               {% if Member.payed == 0 %}
                                                    yellow
                                                {% endif %}">
                                            <td>{{ Member.id }}</td>
                                            <td><img alt="Image" style="height:75px;object-fit: cover" src="{{ asset('uploads/images/profile/') }}{{ Member.ProfileImage }}" class="avatar avatar-lg mt-1">
                                            </td>
                                            <td>{{ Member.Firstname }}</td>
                                            <td>{{ Member.Lastname }}</td>
                                            <td>{{ Member.Email }}</td>
                                            <td>{{ Member.Mobile }}</td>
                                            {% if is_granted('ROLE_USERMANAGEMENT') or is_granted('ROLE_ADMIN') %}
                                                <td>
                                                    <label for="payed">Heeft betaald</label><input class="payed" type="checkbox" style="opacity: 1;pointer-events: all;" {% if Member.payed == 1 %}checked{% endif %}  name="payed" value="{{ Member.id }}"/>
                                                </td>
                                                <td>
                                                    <label for="payed">Is actief</label><input class="Active" type="checkbox" style="opacity: 1;pointer-events: all;" {% if Member.ActivateUser == 1 %}checked{% endif %}  name="Active" value="{{ Member.id }}"/>
                                                </td>
                                            {% endif %}
                                                <td><a class="btn btn-primary" href="{{ path('Member-Change',{MemberId: Member.id}) }}">Change <span class="fas fa-pen-square"></span></a></td>
                                            {% if is_granted('ROLE_USERMANAGEMENT') or is_granted('ROLE_ADMIN') %}
                                                <td>
                                                    <button type="button" id="{{ Member.id }}" class="DeleteCerification btn btn-danger" data-toggle="modal" data-target="#DeleteCerification" data-link="{{ path('Member-Delete',{MemberId: Member.id}) }}">
                                                        Delete <span class="fas fa-times-circle"></span>
                                                    </button>
                                                </td>
                                            {% endif %}

                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                </table>

                            </div>
                        </div>
                    </div>
                </div>
            </div>

        {% endblock %}
{% block modals %}
    <div class="modal fade" id="DeleteCerification" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Bent u zeker?</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                   Ben je zeker dat je de gebruiker wil verwijderen?

                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Sluiten</button>
                    <a class="btn btn-danger" id="modal-link" href="">Verwijderen <span class="fas fa-times-circle"></span></a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

            {% block javascripts %}
                {{ parent() }}




                {{ encore_entry_script_tags('DataTables') }}
                <script>
                    $('.payed').on('click', function (e) {
                        var userid = $(this).val();
                        $.post("{{ path('Payment') }}", {id: userid});
                        $(".tbrw"+userid).toggleClass("yellow")
                    });
                    $('.Active').on('click', function (e) {
                        var userid = $(this).val();
                        $.post("{{ path('Activate') }}", {id: userid});
                    });

                    $('[data-toggle="modal"]').on('click', function (e) {
                        // convert target (e.g. the button) to jquery object
                        var $target = $(e.target);

                        // modal targeted by the button
                        var modalSelector = $target.data('target');
                        // retrieve the dom element corresponding to current attribute
                        var $modalAttribute = $(modalSelector + ' #modal-link');
                        var dataValue = $target.data('link');
                        $modalAttribute.attr('href', dataValue);
                    });

                </script>
            {% endblock %}

